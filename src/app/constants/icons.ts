export enum InfoItemsIcons {
  info_message_icon,
  info_events_icon,
  info_post_icon,
}

export enum Landing_Icons {
  mobile_landing_background
}

export enum Modal_Account_Icons{
  cross
}

export enum Footer_Icons {
  instagram_logo,
  twitter_icon,
  github_icon,
}

export enum Header_Icons {
  bell,
  messages,
}

export enum NavBar_Icons {
  home,
  explore,
  add,
  fire,
}

export enum Post_Icons {
  location,
  'kebab-menu',
  save,
  send,
  comments,
  heart,
}