import { DetectFocusDirective } from './detect-focus.directive';

describe('DetectFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectFocusDirective();
    expect(directive).toBeTruthy();
  });
});
